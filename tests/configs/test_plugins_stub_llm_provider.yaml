plugins:
  llm_provider:
    default:
      module: tests.stubs.stub_llm_provider
      name: StubLLMProvider
      settings:
        prefix: default
    notdefault:
      module: tests.stubs.stub_llm_provider
      name: StubLLMProvider
      settings:
        prefix: notdefault
